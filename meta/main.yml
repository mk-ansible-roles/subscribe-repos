galaxy_info:
  author: Markus Koch
  description: subscribe repositories from reposync server
  license: Apache 2.0
  min_ansible_version: 2.2
  platforms:
  - name: EL
    versions: [ 7 ]

  galaxy_tags: [ 'system', 'reposync', 'beta' ]

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
